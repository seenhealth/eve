---
"eve": patch
---

Include normalized authenticated user identity on `turn.started` events and OpenTelemetry turn/model-call attributes. Built-in message channels provide presentation names when available, while consumers treat the user ID as canonical.
