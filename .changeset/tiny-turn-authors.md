---
"eve": patch
---

Include normalized authenticated user identity on `turn.started` events and OpenTelemetry turn/model-call attributes. Consumers can show durable per-turn authorship while treating the user ID as canonical and the optional name as a presentation snapshot.
